# R2.4 — `apply_batch`, the generic batch operation contract

Last item of `TASKS.md`'s *"generic page/metadata/batch"* line. Applies many node
mutations in one call, against node IDs that already exist, with a typed per-operation
receipt. Design and rationale: [`BATCH-CONTRACT-PLAN.md`](BATCH-CONTRACT-PLAN.md).

**Status: ACCEPTED 2026-08-18.** `apply_batch` is `resultStability: "stable"`, promoted
at acceptance per the R1 precedent after three live gates earned it — 5.5 (2026-08-12),
5.6 (2026-08-18) and the 4.1 re-run on the accepted build. Every item this note listed as
owed is closed: 3.1 chunked progress, 3.2 the measured sleep default, Phase 4 additive
alignment, and 5.6 including the pin assigned below.

⛔ `stable` means frozen. A change to the receipt shape now needs a new
`publicContractVersion`, not just a rebuild — and the walk-back to `additive-preview` is a
breaking change `compatibilityErrors()` rejects by name.

## Runtime identity

| | R2.4 phase 2 (gated 5.5) | R2.4 ACCEPTED (this build) |
| --- | --- | --- |
| Public contract / server schema / plugin API | `1.5.0` | **`1.6.0`** |
| Server build | `r2-server-9239fd0bc71b` | **`r2-server-5ac4bcd1a2a5`** |
| Plugin build | `r2-plugin-d0342abb6c4a` | **`r2-plugin-53a1fa676d6a`** |
| Capability fingerprint | `sha256:a87b5d98…835704` | **`sha256:d39aefef…ca6289`** |
| Tools / prompts / plugin commands | 53 / 6 / 52 | **53 / 6 / 52** |

⚠️ **The server half moved twice after 5.6 and the fingerprint moved neither time.** 4.1
was a wrapper-only change and the promotion to `stable` is contract metadata, so the
capability IDs and `serverSchemaVersion` the fingerprint hashes never changed — a stale
`dist/server.js` sails straight past the fingerprint check. `serverBuildId` is the only
pin that catches it, which is why the live gate pins the build and not just the pair.
⭐ The plugin build is **unchanged since 1.6.0**: adopting this build needs a server
respawn, not a DEV plugin re-run.

New capability ID: `figma.command.apply_batch@1`. `timeoutClass: "heavy_batch"` (rank 4 —
a batch scales with its *arguments*, not with the file). `pluginUpdates: "none"`, which is
true today and is also what currently gives the batch a real ceiling, since nothing resets
the inactivity timer.

## The contract

```
apply_batch(operations[], onError?, prevalidateOnly?, timeBudgetMs?, maxResultBytes?)
```

Every target is resolved in one **total** pass before anything is written, so
`onError: "stop"` refuses the whole batch without mutating and `prevalidateOnly` is a real
dry run. v1 is **mutate-only**: an operation may only name a node ID that already exists,
and every `create_*` is pinned absent by a test.

Two refusals **throw** instead of returning a receipt — a duplicate `id` makes correlation
undefined, and an unknown `op` has no entry shape, so neither can be reported inside the
structure it breaks. Everything below the envelope is reported *in* a well-formed receipt.

⛔ **Operations are NOT atomic.** Nine of the fifteen allowlisted mutations write more than
one field. A `failed` receipt carries `partialApplicationPossible` and, when true, the
recorded reason — so a caller re-reads the node instead of assuming its request was a
no-op.

## Offline gate

98 tests (`node --test`), `bun run verify` → *"R2 offline gate passed: 53 tools, 6 prompts,
source/runtime parity verified"*, five baselines (R0, R1, R2.1, R2.2, R2.3) replaying at
zero errors, and a post-commit rebuild leaves the tree clean — the committed `dist/` is
reproducible.

## Connected gate — PASSED 2026-08-12

`scripts/live-batch-gate.mjs`, channel `8fbuzws2`, on the SYD throwaway copy. **Run twice,
green both times** (`R2.4 Gate 20260812134015` and `…134153`). Both runs restored the
document to its 6-page baseline id-for-id with `currentPageId` back at `1068:5433` — and
run 2 reading that same baseline is the independent proof that run 1 left nothing behind,
which self-cleanup alone never establishes.

`dist/server.js` and `src/cursor_mcp_plugin/code.js` were hashed **before** the run
(`6a876855…dc0ccb`, `37860d7e…9e7b77a`), so the artifact exercised is the artifact
documented. Live join reported the exact pinned pair, `compatibility: "compatible"`,
`issues: []`, 53 tools.

| Check | Result |
| --- | --- |
| Dry run against real content | `prevalidated`, 3 ops all `skipped`, `complete: true`, 1 ms |
| One bad target under `stop` | `refused_prevalidation`, 0 succeeded / 3 skipped, nothing written |
| Same batch under `continue` | `partial`, 2 succeeded / 1 skipped / 0 failed |
| Duplicate `id` | refused in 4 ms, `duplicate_operation_id` in the message |
| Disallowed `op` | refused in 2 ms, at the **schema** layer |
| Non-atomicity | `partial`, 3 failed — all three partial applications **observed** |
| Destructive scope | `all_succeeded`, reported `childCount: 3`, subtree gone afterwards |
| Plugin after the batch | `get_runtime_info` answered `compatible` in 4 ms — no wedge |

### Total prevalidation, observed on real content

The dry run resolved real node `9:2` ("Frame 1", `FRAME`) on page `0:1`, reporting
`childCount: 1` for **a `delete_node` operation it did not perform** — D7's blast-radius
promise, observed on a node we were not willing to lose. The scratch leaf resolved to
`childCount: null`, not `0`: *"cannot have children"* is not *"has none"*. Re-reading the
real page afterwards found the node present and its name unchanged, so `prevalidateOnly`
wrote nothing.

### Atomic refusal, then honest partial

The same three operations — a rename, a `set_fill_color` at a nonexistent id, a text write
— were submitted twice. Under `stop` the batch was refused whole (`node_not_found` on the
bad target, every operation `skipped`) and read-backs confirmed the rect was still
`gate-probe-rect` and the text still `before`. Under `continue` the unresolvable target was
skipped and the other two applied, confirmed by read-back. The bad target sat in the
**middle** of the batch, so prevalidation totality does not depend on ordering.

### The two envelope refusals arrive differently

- **Duplicate `id`** — the plugin throws; the tool wrapper catches it into an error
  *result*. Recorded layer: `handler`.
- **Disallowed `op`** — the tool's inline `z.enum` rejects it before dispatch, so it is a
  genuinely thrown protocol error listing all fifteen allowed operations. Recorded layer:
  `schema`.

`BATCH_ERROR_CODE_DELIVERY` calls both *"thrown"*, which is true at the module boundary;
through this transport only the second is literally thrown. A result-only harness would
mis-score both, which is why the gate records the layer instead of flattening it.

### Non-atomicity is now observed, not just declared

All three probes reproduced a partial application against a real file, in one `continue`
batch that still reported `complete: true`:

| Operation | Observed change under a `failed` receipt | Rejected by |
| --- | --- | --- |
| `set_item_spacing` | child gap 16 → **24** | the handler's own `layoutWrap !== "WRAP"` check |
| `move_node` | `x` 0 → **120** | Figma: *in set_y: Property "y" failed validation: Expected number, received string* |
| `set_stroke_color` | `strokes` none → **SOLID `#ff0000`** | Figma: *in set_strokeWeight: … Expected number, received string* |

⭐ **Two of the six *unproven* non-atomic operations are now proven.** `move_node` and
`set_stroke_color` were listed rather than demonstrated, on the stated hypothesis that *"a
platform-level rejection on a later field leaves the earlier ones applied."* Both now
reproduce exactly that, at the Figma property setter rather than in fork code — the
hypothesis is observed, not assumed. Five of the nine are now proven.

⚠️ `itemSpacing` has **no read path**: both copies of `filterFigmaNode` drop it, so no tool
surfaces it. The gate reads it back indirectly as the gap between two auto-layout children,
whose `absoluteBoundingBox` does survive the filter.

Truncation was exercised in the same batch: `maxResultBytes: 8` cut the succeeding
operation's result while still reporting its true size (`resultBytes: 77`,
`resultTruncated: true`).

## What the gate found

1. ✅ **The `apply_batch` description was wrong about `params`** — claiming *"Same shape as
   the standalone tool of the same name"* when batch params go straight to the plugin
   handler: `set_fill_color` and `set_stroke_color` take `{color:{r,g,b,a}}` in a batch and
   flat `r,g,b,a` as standalone tools. **Fixed in `664135b` with 3.1**, as planned.

   🔴 **But the gate kept reporting it for two more runs.** It hard-coded the quoted
   sentence as a finding instead of reading the description, so it filed a defect that no
   longer existed — and nothing noticed, because a finding does not move the verdict. The
   gate now asserts the **published** `listTools` schema declares the handler shape
   (`applyBatchDeclaresHandlerParamShape`) and that the old sentence is absent.
   ⭐ A finding is not evidence. Check when it was last verified against the thing it
   describes, or it becomes folklore that outlives its own fix.
2. **`params` is `z.record(z.any())`, so per-operation arguments get no schema
   validation.** A wrong-shaped param fails plugin-side and arrives as a receipt entry
   rather than a schema throw. Defensible for a batch — a receipt beats an exception — but
   it is a real asymmetry with the standalone tools and belongs in the description.
3. **`operation_not_allowed` is unreachable through this transport.** The allowlist is
   enforced twice and the outer layer answers first, so the plugin's own check never
   refuses a live consumer. The code stays because the plugin is reachable by other
   clients, but no consumer will observe it here.

## Acceptance checklist — all closed 2026-08-18

- ✅ **3.1** chunked progress, with `pluginUpdates` moved off `"none"` in the same change so
  it did not recreate Finding 4, and `timeBudgetMs` still binding so Finding 5 stayed
  closed. Observed live: 15 ops at chunk 5 → 4 frames (0→33→67→100).
- ✅ **3.2** the measured inter-chunk sleep default. Measured against a real file rather
  than assumed: 1828 ms observed for a 1000 ms pause over 2 gaps. Every operation
  succeeded at `chunkPauseMs=0`, so the `0` default is **earned**, not guessed.
- ✅ **Phase 4** additive alignment of `set_multiple_text_contents`,
  `set_multiple_annotations` and `delete_multiple_nodes` — every legacy field unchanged,
  all five baselines still replaying.
  - ✅ **4.1 reaches a live consumer on all three.** It previously reached only
    `delete_multiple_nodes`: the other two formatted prose and discarded the unified
    fields in the MCP wrapper, and the offline suite passed because it pinned the plugin
    handler — the layer that had changed. Now asserted end-to-end offline
    (`tests/wrapper-end-to-end.test.mjs`) **and** asserted by the live gate, which no
    longer merely records `unifiedFieldsVisibleToConsumer`.
- ✅ **5.6** the new pin is assigned in the Runtime identity table above. Consumers adopt
  on their own schedule; the plugin half has not moved since 1.6.0.
- ✅ Promoted `additive-preview` → `stable` **at acceptance**, then re-gated: the promotion
  moves the contract and therefore the server build, so the accepted build was re-pinned
  and the live gate re-run on it. ⛔ Accepting a build the gate never saw would have been
  the same defect this release spent three phases closing.

## Still true, and deliberately not fixed

- **`operation_not_allowed` stays unreachable through this transport.** Recorded, not
  asserted — the outer `z.enum` answers first.
- **`params` gets no schema validation.** Declared in the param description; a wrong shape
  arrives as a failed receipt entry rather than a schema throw.
- **Per-op atomicity is FALSE and observed on three ops** — `move_node`,
  `set_stroke_color`, `set_item_spacing`, each rejected by the *Figma property setter*
  rather than by the envelope.
